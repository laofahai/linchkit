/**
 * Ontology — unified semantic facade factory + descriptor types (server).
 */

export {
  createOntologyRegistry,
  type EntityDescriptor,
  type OntologyRegistry,
  type OntologyRegistryDeps,
  type RelationDescriptor,
} from "../../ontology";
