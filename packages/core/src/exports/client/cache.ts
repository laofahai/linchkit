/**
 * Cache types (browser-safe).
 * Runtime cache providers live in ../server/cache.ts.
 */

export type {
  CacheEntry,
  CacheManagerOptions,
  CacheProvider,
  CacheSetOptions,
  CacheStats,
  InMemoryCacheOptions,
  NamespacedCache,
} from "../../cache";
