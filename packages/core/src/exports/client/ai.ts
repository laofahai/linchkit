/**
 * AI types (browser-safe).
 * Runtime AI classes live in ../server/ai.ts.
 */

export type {
  AIActionAccess,
  AIBoundaryCheckResult,
  AIBoundaryOptions,
  AIBudget,
  AIBudgetConfig,
  AICallRequest,
  AIContentFilter,
  AIPolicy,
  AIRateLimits,
  AIUsageRecord,
} from "../../ai";
