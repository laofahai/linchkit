/**
 * Shared UI constants for consistent styling across components.
 */

/** Standard class for icon-only toolbar buttons — keeps all toolbar buttons the same height. */
export const TOOLBAR_ICON_BTN = "h-7 w-7 p-0";
